# Notice

minmax 额度查看器

在设置里输入apiKey即可。 本人并不清楚这样的默认保存是否安全（但代码没有问题）

# 使用

`make clean`

清除安装

`make install`

安装到本地小组件文件夹

`make run`

执行安装后额外启动一个预览程序

## 一键安装

```
git clone https://github.com/cmachsocket/minmax-token-monitor.git
cd minmax-token-monitor
make install
```


